#include "pintos_thread.h"


/* must include the number of open seats for the train, 
   the number of waiting passengers, and the number of
   boarding passengers */
struct station 
{
  /* No condition variable here, because of the */
  unsigned int seats_open;
  struct lock seat_lock;

  unsigned int pass_waiting;
  struct lock pass_lock;
  struct condition pass_queue;

  

};

void
station_init(struct station *station)
{
  
  

}

void station_load_train(struct station *station, int count)
{

  // there should be a loop in here first checking if 
  // there are any open seats, and secondly if there are
  // any passengers waiting. 

}


/* Let's just have the passengers wait immediately as they arrive*/
void station_wait_for_train(struct station *station)
{

}

void
station_on_board(struct station *station)
{

}
